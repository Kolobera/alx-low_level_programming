#include "main.h"

/**
 * print_square - in the name
 * @size: parameter
 * Retutn: None
 */

void print_square(int size)
{
	int ch, i;

	for (ch = 0; ch < size; ch++)
	{
		for (i = 0; i < size; i++)
			_putchar(35);
		_putchar('\n');
	}
	if (n <= 0)
		_putchar('\n');
}
