#include <stdio.h>
#include "main.h"

/**
 * _abs - Calculate the valeur absolue
 *
 *@n: integer
 *
 * Return: The absolute val
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
